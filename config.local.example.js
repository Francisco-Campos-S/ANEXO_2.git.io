/**
 * Copie este archivo como `config.local.js` en la misma carpeta que index.html
 * y pegue SOLO en su equipo la clave de Google AI Studio.
 *
 * config.local.js está en .gitignore y no debe subirse a GitHub.
 *
 * Alternativa sin archivo: en la consola del navegador (F12):
 *   localStorage.setItem('GEMINI_API_KEY', 'SU_CLAVE_AQUI');
 * y recargue la página.
 */
window.GEMINI_API_KEY = '';
